# Production .env on VPS — copy to /var/www/barberbook/.env

NODE_ENV=production
PORT=5100
BASE_URL=https://your-domain.com

# Generate: openssl rand -hex 32
SESSION_SECRET=CHANGE_ME_LONG_RANDOM

DATABASE_URL=file:./database.sqlite

# Email — on many VPS hosts SMTP (587) is blocked; use Gmail API:
# See deploy/GMAIL-API-SETUP.md
EMAIL_USE_GMAIL_API=true
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret
GMAIL_REFRESH_TOKEN=your-refresh-token
EMAIL_FROM="Your Shop Name <noreply@your-domain.com>"
EMAIL_USER=noreply@your-domain.com

# Local dev / fallback SMTP (optional)
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_PASS=YOUR_APP_PASSWORD

# Google Calendar JSON (optional) — or configure in Admin UI
